# 🧠 RAG Knowledge Base Engine

Микросервис для создания собственной системы вопросов-ответов на базе ваших документов (PDF, TXT) с использованием LLM.

## 🎯 Задача

Компании часто имеют внутренние базы знаний (регламенты, инструкции, спецификации), но поиск по ним занимает часы. Этот инструмент позволяет:
- 📄 **Загружать документы** (PDF, TXT, Docx)
- 🧠 **Создавать векторный индекс** (FAISS) для быстрого семантического поиска
- 🤖 **Отвечать на вопросы** на естественном языке, ссылаясь на точные источники

**Результат:** менеджеры вместо поиска в документах спрашивают бота и получают ответ за 2 секунды.

## 💡 Как это работает

```
Загрузка документов (PDF/TXT)
         ↓
Разбивка на смысловые чанки (chunking)
         ↓
Преобразование в векторы (embeddings via OpenAI)
         ↓
Сохранение в FAISS индекс
         ↓
Поиск топ-3 релевантных чанков при вопросе
         ↓
Генерация ответа через GPT-4o-mini с контекстом
         ↓
Результат + ссылки на источники
```

## 🛠 Технический стек

| Компонент | Технология | Зачем |
| :--- | :--- | :--- |
| **Document Loading** | LangChain, PDFPlumber | Загрузка PDF, TXT |
| **Text Splitting** | RecursiveCharacterTextSplitter | Умная разбивка на чанки (500 символов) |
| **Embeddings** | OpenAI Embeddings | Преобразование текста в векторы |
| **Vector DB** | FAISS | Быстрый семантический поиск |
| **LLM** | GPT-4o-mini | Генерация ответов (дешево!) |
| **Chains** | LangChain RetrievalQA | Оркестрация всего процесса |

## 🚀 Быстрый старт

### Локально

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Установите OPENAI_API_KEY:**
   ```bash
   export OPENAI_API_KEY="sk-..."
   ```

3. **Запустите пример:**
   ```bash
   python rag_engine.py
   ```

4. **Результат:**
   ```
   ✅ Загружен документ: sample_knowledge.txt
   ✅ Разбито на 12 чанков
   ✅ Индекс создан и готов к использованию
   
   ❓ Вопрос: Во сколько начинается рабочий день?
   ✅ Ответ: Рабочий день начинается в 09:00 и заканчивается в 18:00.
   📄 Источники: sample_knowledge.txt
   ```

### С собственными документами

```python
from rag_engine import RAGEngine

# Инициализация
rag = RAGEngine()

# Загрузка ваших документов
docs = rag.load_documents([
    "regulament.pdf",
    "instructions.txt",
    "specs.pdf"
])

# Создание индекса
rag.build_index(docs)

# Запросы
answer, sources = rag.query("Как подать заявку на отпуск?")
print(f"Ответ: {answer}")
print(f"Источники: {sources}")

# Сохранение индекса для переиспользования
rag.save_index("my_knowledge_base")
```

### На облачной платформе (Render, Railway)

1. Создайте репо на GitHub
2. Добавьте `OPENAI_API_KEY` в секреты облачного сервиса
3. Деплойте!

## 📋 API RAGEngine

```python
class RAGEngine:
    def load_documents(file_paths: List[str]) -> List[Document]
    # Загрузить документы (txt, pdf)
    
    def build_index(documents, chunk_size=500, chunk_overlap=100) -> None
    # Создать FAISS индекс
    
    def query(question: str) -> Tuple[str, List[str]]
    # Ответить на вопрос + вернуть источники
    
    def save_index(path: str) -> None
    # Сохранить индекс на диск
    
    def load_index(path: str) -> None
    # Загрузить сохраненный индекс
```

## 🎓 Что демонстрируется

- ✅ **RAG архитектура** — классический паттерн для LLM-систем
- ✅ **Vector embeddings** — преобразование текста в семантические векторы
- ✅ **Semantic search** — поиск по смыслу, не по ключевым словам
- ✅ **Chunking & Splitting** — умная разбивка больших документов
- ✅ **LangChain интеграция** — оркестрация RAG pipeline
- ✅ **Source attribution** — ссылки на точные источники в ответах

## 🔐 Безопасность

- Храните API ключи в переменных окружения
- Используйте `.env` файл локально (не выкладывайте!)
- На облаке используйте Secrets管理 сервис

## 🚨 Важно

- **API ключи:** Никогда не коммитьте `OPENAI_API_KEY` в код!
- **FAISS индекс:** Может быть большим (100+ МБ). Сохраняйте локально.
- **Chunking:** Параметры `chunk_size` и `chunk_overlap` важны для качества.

## 📚 Примеры использования

### Пример 1: Регламент компании
```python
rag.load_documents(["company_regulations.pdf"])
rag.build_index(documents)

answer, _ = rag.query("Какие правила безопасности?")
# Ответ с прямыми ссылками на регламент
```

### Пример 2: Техническая документация
```python
rag.load_documents(["api_docs.txt", "architecture.pdf"])
rag.build_index(documents)

answer, sources = rag.query("Как интегрировать OAuth?")
# Точный ответ из документации
```

---

**Создано:** Илья (AI-консультант)  
**Стек:** LangChain · OpenAI · FAISS · Python 3.9+